---
title: Quick Start Guide
description: This section provides an overview of how to quickly get started using the TMDB wrapper.
icon: Rocket
---

<Callout>
	You need a TMDB credential to use this library. The client supports both a **Bearer token** (JWT Read Access Token, recommended) and a
	legacy **v3 API key**. See the [Authentication guide](/docs/getting-started/authentication) for details.
</Callout>

References:

- https://www.themoviedb.org/settings/api - Your API key and Bearer token
- https://developer.themoviedb.org/docs/getting-started - Getting Started Guide

### Install

```npm
npm install @lorenzopant/tmdb
```

### Create TMDB object

Pass your Bearer token or v3 API key as the first argument:

```ts title=tmdb.ts
// With a Bearer token (recommended)
export const tmdb = new TMDB(process.env.TMDB_BEARER_TOKEN!);

// With a v3 API key
export const tmdb = new TMDB(process.env.TMDB_API_KEY!);
```

### Use TMDB methods

Once you've initialized the `TMDB` client, you can start using the API methods. Here's how to search for a movie:

```ts
const results = await tmdb.search.movies({ query: "Inception" });
console.log(results.results);
```

You’ll get a paginated response with a list of matching movies, including their titles, poster paths, IDs, and other metadata.

### What's Next?

<Cards>
	<Card title="Authentication" href="/docs/getting-started/authentication">
		Learn about Bearer tokens vs v3 API keys
	</Card>
	<Card title="Features" href="/docs/getting-started/features">
		Deduplication, error handling, logging, and more
	</Card>
	<Card title="API Reference" href="/docs/api-reference">
		Discover all available methods
	</Card>
</Cards>

For the full reference, visit the [TMDB Developer Documentation](https://developer.themoviedb.org/docs) and check out each endpoint.
